import { MoreHorizontal, Send } from "lucide-react";
import type { ForumDmConversation, ForumDmMessage } from "../../../types";
import { ForumAvatar } from "./ForumAvatar";

export function ForumDmConversation({ conversation, messages, body, setBody, sending, onSend, onDelete }: { conversation: ForumDmConversation; messages: ForumDmMessage[]; body: string; setBody: (value: string) => void; sending: boolean; onSend: () => void; onDelete: () => void }) {
  return <><header className="flex shrink-0 items-center justify-end border-b border-slate-100 bg-white px-3 py-2"><button type="button" aria-label="删除会话" onClick={onDelete} className="flex h-9 w-9 items-center justify-center rounded-full text-slate-500 hover:bg-slate-100"><MoreHorizontal className="h-5 w-5" /></button></header><main className="min-h-0 flex-1 overflow-y-auto bg-slate-50 px-3 py-4"><div className="space-y-3">{messages.sort((a, b) => a.occurredAt - b.occurredAt).map((message) => <div key={message.id} className={`flex ${message.sender === "user" ? "justify-end" : "justify-start"}`}>{message.sender === "participant" && <ForumAvatar author={conversation.participantPublicSnapshot} className="mr-2 h-8 w-8" />}<p className={`max-w-[78%] whitespace-pre-wrap break-words rounded-2xl px-3 py-2 text-sm leading-5 ${message.sender === "user" ? "bg-neutral-950 text-white" : "bg-white text-slate-800 shadow-sm"}`}>{message.body}</p></div>)}</div></main><footer className="shrink-0 border-t border-slate-100 bg-white px-3 pb-[max(10px,env(safe-area-inset-bottom))] pt-2"><div className="flex items-end gap-2"><textarea value={body} onChange={(event) => setBody(event.target.value)} rows={1} maxLength={2000} placeholder="发送论坛私信…" className="max-h-24 min-h-10 flex-1 resize-none rounded-2xl border border-slate-200 bg-slate-50 px-3 py-2.5 text-sm outline-none" /><button type="button" disabled={!body.trim() || sending} onClick={onSend} className="flex h-10 w-10 items-center justify-center rounded-full bg-neutral-950 text-white disabled:bg-slate-200"><Send className="h-4 w-4" /></button></div></footer></>;
}
