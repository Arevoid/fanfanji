/**
 * Offline story dialogue is never injected into online chat automatically.
 * Only the explicit archive/sync workflow may convert a concise story summary
 * into the normal relation-scoped memory vault.
 */
export function getOfflineStoriesContextForOnlineChat(): string {
  return "";
}
